#pragma once
#include "raylib.h"

void doNothingMachine(); // Does Nothing

int bint(bool b);  // turns a bool to an int true == 1 && false == 0

float max(float num, float max); // Finds the greater of two values

float min(float num, float max); // Finds the lesser of two values